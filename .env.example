# =============================================================================
# OS Ghost - Environment Variables Example
# =============================================================================
# Copy this file to `.env` and fill in the values.

# ---------------------------------------------------
# APPLICATION KEYS
# ---------------------------------------------------
# Gemini API Key (Required for AI puzzle generation)
GEMINI_API_KEY=your-gemini-api-key-here

# GitHub Personal Access Token (for version bump workflow)
GH_OWNER_TOKEN=your-github-pat-token

# ---------------------------------------------------
# CERTIFICATE EXPORT CONFIG (For scripts/export_cert.py)
# ---------------------------------------------------
# SHA-1 hash of Apple Distribution/Development certificate
APPLE_CERT_HASH=your_certificate_hash_here
# Password for exporting the .p12 file
APPLE_CERT_EXPORT_PASS=your_strong_export_password
# Temporary keychain password (used during export process)
APPLE_TEMP_KEYCHAIN_PASS=temp123

# ---------------------------------------------------
# CI/CD SECRETS (Copy to GitHub Actions Secrets)
# ---------------------------------------------------
# These are generated by 'npm run tauri signer generate' and 'scripts/export_cert.py'

# Tauri Updater Signing (Required for auto-updates)
# TAURI_PRIVATE_KEY=<base64 encoded private key>
# TAURI_KEY_PASSWORD=<password used during key generation>

# Apple Code Signing (Required for macOS builds)
# APPLE_CERTIFICATE=<base64 encoded .p12 file>
# APPLE_CERTIFICATE_PASSWORD=<.p12 export password>
# APPLE_SIGNING_IDENTITY=<e.g., "Apple Distribution: Your Company (TEAMID)">

# Keychain Password (Required for macOS CI keychain setup)
# KEYCHAIN_PASSWORD=<any secure password for CI keychain>
